venv/
.env
__pycache__/
*.pyc
data/known_faces/*.pkl
*.pt
*.h5
frontend/node_modules/
frontend/dist/

# Face images are biometric data: never commit them. Each person's reference
# photos and all evaluation images stay on the machine that uses them.
# (Files already tracked are not affected by these rules.)
data/known_faces/*/
data/test_images/
data/test_videos/
!data/README.md

# Partial downloads and local tool config
*.part
.claude/

*.dump
