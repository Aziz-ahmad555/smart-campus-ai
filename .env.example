# Copy to .env (never commit .env) and fill in your values.

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_campus_db
DB_USER=postgres
DB_PASSWORD=change-me

# Sessions end this many hours after sign-in
SESSION_HOURS=8
